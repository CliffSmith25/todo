import React from 'react';
import { render } from 'react-dom';
import App from '../App';
import { Provider } from 'react-redux'
import store from '../../store'

test('non-obvious test when it crashes', () => {
  expect(true).toEqual(true)
})
